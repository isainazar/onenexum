import { actionTypes } from "../types/actionTypes";

export const ProfileReducer = (state, action) => {
/*   if (action.type === actionTypes.p1_completed) {
    let datos = action.payload;
    datos.profile.p1 = true;
    localStorage.setItem("pro", JSON.stringify(datos));
    return datos;
  } */
  return state
};
